---

ignore_docker_version: true

nodes:
  - address: home.dbnet.com.au
    user: frankc
    role: [controlplane,worker,etcd]

services:
  etcd:
    snapshot: true
    creation: 6h
    retention: 24h

